import Title from './components/Title';
// we will use setup for all the filename but always change the file path to the specific component
import Setup from './examples/6-interceptors'
// import './axios/global'


function App() {
  return (
    <main>
      <Title />
      <Setup />
    </main>
  );
}

export default App;
